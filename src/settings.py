root_directory = '/usr/local/var/springer-recommendations'

default_number_of_partitions = 4 # for best performance this should be the same as the total number of disco workers (check the disco web config)
